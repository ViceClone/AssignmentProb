{
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build active file",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "${workspaceFolder}/src/maintest.cpp",
        "${workspaceFolder}/src/Population.cpp",
        "-o",
        "test"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}